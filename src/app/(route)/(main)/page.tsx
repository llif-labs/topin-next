'use client'

import {useEffect} from 'react'
// import API from '@/core/module/service/api'

const Page = () => {

  useEffect(() => {
    // API.call({
    //   url: '/api/issue/1',
    //   method: 'get',
    // }).then((res) => {
    //   console.log(res)
    // })
  }, [])

  return<div>
    123
  </div>
}

export default Page
